# 复制为 .env 后填写本地配置，不要在示例文件中保存真实密钥。

# DeepSeek 模型
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-flash
DEEPSEEK_TIMEOUT_SECONDS=60
DEEPSEEK_MAX_RETRIES=2
CREDENTIAL_VALIDATION_TIMEOUT_SECONDS=15

# 模型访问模式。BYOK 默认关闭，启用后用户可以保存自己的 DeepSeek Key。
BYOK_ENABLED=false
PLATFORM_MODEL_ENABLED=true
DEFAULT_MODEL_MODE=PLATFORM

# 用户模型凭据加密主密钥环。值为32字节随机数的 URL-safe Base64 编码。
# 真实值只能保存在服务端环境变量、Vault 或 KMS 中，不能提交到 Git。
PROVIDER_CREDENTIAL_MASTER_KEYS=
PROVIDER_CREDENTIAL_ACTIVE_KEY_VERSION=v1

# 账号 Session、登录保护与业务接口限流
AUTH_SESSION_TTL_HOURS=168
AUTH_SESSION_TOUCH_INTERVAL_SECONDS=300
AUTH_LOGIN_MAX_FAILURES=5
AUTH_LOGIN_WINDOW_SECONDS=900
REGISTRATION_MODE=closed
AUTH_REGISTRATION_MAX_FAILURES=10
AUTH_REGISTRATION_WINDOW_SECONDS=900
AUTH_INVITATION_MAX_TTL_HOURS=720
API_RATE_LIMIT_ENABLED=true
API_RATE_LIMIT_REQUESTS=60
API_RATE_LIMIT_WINDOW_SECONDS=60

# 基础设施模式。local 保持 SQLite/Chroma/本地文件；production 使用以下共享服务。
INFRASTRUCTURE_MODE=local
# compose.production.yml 的容器初始化凭据。仅允许 URL-safe 字符，生产环境请替换。
POSTGRES_PASSWORD=replace-with-a-long-url-safe-password
REDIS_PASSWORD=replace-with-a-long-url-safe-password
MINIO_ROOT_USER=lifepilot-admin
MINIO_ROOT_PASSWORD=replace-with-a-long-url-safe-password
DATABASE_URL=
CHECKPOINT_DATABASE_URL=
DATABASE_POOL_SIZE=10
DATABASE_MAX_OVERFLOW=20
DATABASE_POOL_RECYCLE_SECONDS=1800

# Redis 共享限流
REDIS_URL=
REDIS_KEY_PREFIX=lifepilot

# S3 兼容对象存储（开发环境可使用 MinIO）
OBJECT_STORAGE_ENDPOINT_URL=
OBJECT_STORAGE_REGION=us-east-1
OBJECT_STORAGE_BUCKET=lifepilot-knowledge
OBJECT_STORAGE_ACCESS_KEY=
OBJECT_STORAGE_SECRET_KEY=
OBJECT_STORAGE_SECURE=true
OBJECT_STORAGE_SSE=AES256

# Celery 后台知识索引
CELERY_BROKER_URL=
KNOWLEDGE_WORKER_QUEUE=knowledge
KNOWLEDGE_TASK_MAX_RETRIES=3
EMBEDDING_DIMENSION=512
THREAD_LOCK_WAIT_SECONDS=5

# Agent 单次运行最大图执行步数
AGENT_RECURSION_LIMIT=25

# CLI 本地身份与默认会话（Web/API 使用登录用户身份）
LOCAL_CLI_OWNER_ID=local-user
DEFAULT_THREAD_ID=main

# SQLite 数据库
APP_DATABASE_PATH=data/lifepilot.db
CHECKPOINT_DATABASE_PATH=data/checkpoints.db

# 应用日志
LOG_LEVEL=INFO
LOG_FILE_PATH=logs/lifepilot.log
LOG_MAX_BYTES=1000000
LOG_BACKUP_COUNT=3

# 运行环境与 LangSmith 追踪
APP_ENVIRONMENT=development
LANGSMITH_TRACING=false
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=lifepilot-development
LANGSMITH_ENDPOINT=https://apac.api.smith.langchain.com
LANGSMITH_WORKSPACE_ID=
LANGSMITH_HIDE_INPUTS=false
LANGSMITH_HIDE_OUTPUTS=false

# LangGraph Checkpoint 序列化
LANGGRAPH_STRICT_MSGPACK=true

# 个人知识库路径
KNOWLEDGE_SOURCE_DIRECTORY=knowledge_base
CHROMA_PERSIST_DIRECTORY=data/chroma

# 本地中文 Embedding 模型
EMBEDDING_MODEL_NAME=models/bge-small-zh-v1.5
EMBEDDING_DEVICE=cpu

# RAG 切块、检索和文件限制
KNOWLEDGE_CHUNK_SIZE=700
KNOWLEDGE_CHUNK_OVERLAP=120
KNOWLEDGE_RETRIEVAL_K=4
KNOWLEDGE_MAX_FILE_BYTES=20000000

# Streamlit 后端地址
LIFEPILOT_API_URL=http://127.0.0.1:8000
